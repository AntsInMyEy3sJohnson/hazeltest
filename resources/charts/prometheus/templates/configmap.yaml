apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "prometheus.name" . }}-config
  labels:
    app.kubernetes.io/name: {{ template "prometheus.name" . }}-config
    app.kubernetes.io/version: {{ .Chart.Version }}
    app.kubernetes.io/instance: {{ .Release.Name }}
data:
{{ toYaml .Values.prometheusServer.config | indent 2 }}